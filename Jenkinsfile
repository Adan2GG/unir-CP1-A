pipeline {
    agent any

     stages {

        stage('Build') {
           steps {
              echo 'Eyyy, esto es Python. No hay que compilar nada!!!'
	          echo WORKSPACE
              sh 'ls -la'
           }
        }
     }
}
